@model IEnumerable<StudyTweets.backend>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.backendname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.backendname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.backendid }) |
            @Html.ActionLink("Details", "Details", new { id=item.backendid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.backendid })
        </td>
    </tr>
}

</table>
